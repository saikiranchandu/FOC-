#include<stdio.h>
int main()
{
	int n,i,sum=0;
	printf("Enter the N Number");
	scanf("%d ",&n);
	for(i=2;i<=n;i++)
	{
		if(i%2==0)
		{
			printf(" %d",i);
		sum=sum+i;
	    }
	    else
	    {
	    	printf(" ");
		}
	}
	printf(" \n The sum is %d",sum);
}
